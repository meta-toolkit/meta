/**
 * @file select_info_gain.cpp
 */

#include "classify/select_info_gain.h"

using std::vector;

vector<TermID> classify::feature_select::info_gain(const vector<Document> & docs)
{
    vector<TermID> features;
    return features;
}
